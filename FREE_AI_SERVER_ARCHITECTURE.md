# 완전 무료 AI API 서버 아키텍처

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    Client (Web/App)                      │
│              Next.js / React / Mobile App                │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP/REST API
                       │
┌──────────────────────▼──────────────────────────────────┐
│              FastAPI Server (Port 8000)                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │  API Router Layer                                │   │
│  │  - /generate/text                                │   │
│  │  - /generate/code                                │   │
│  │  - /generate/prompt                              │   │
│  │  - /health                                       │   │
│  └──────────────┬───────────────────────────────────┘   │
│                 │                                        │
│  ┌──────────────▼───────────────────────────────────┐   │
│  │  Service Layer                                   │   │
│  │  - TextGenerator                                 │   │
│  │  - CodeGenerator                                 │   │
│  │  - PromptOptimizer                               │   │
│  └──────────────┬───────────────────────────────────┘   │
│                 │                                        │
│  ┌──────────────▼───────────────────────────────────┐   │
│  │  Ollama Client Layer                             │   │
│  │  - Model Manager                                 │   │
│  │  - Request Queue                                │   │
│  │  - Response Cache                                │   │
│  └──────────────┬───────────────────────────────────┘   │
└──────────────────┼──────────────────────────────────────┘
                   │ HTTP API (localhost:11434)
                   │
┌──────────────────▼──────────────────────────────────────┐
│              Ollama Server (Port 11434)                  │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Local LLM Models                                │   │
│  │  - llama3.1:8b (텍스트 생성)                     │   │
│  │  - deepseek-coder:6.7b (코드 생성)              │   │
│  │  - qwen2.5:7b (다목적)                          │   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

## 📁 서버 디렉토리 구조

```
free-ai-server/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 앱 진입점
│   ├── config.py               # 설정 관리
│   ├── models/
│   │   ├── __init__.py
│   │   ├── request.py          # 요청 스키마
│   │   └── response.py         # 응답 스키마
│   ├── services/
│   │   ├── __init__.py
│   │   ├── ollama_client.py    # Ollama 연동
│   │   ├── text_generator.py   # 텍스트 생성
│   │   ├── code_generator.py   # 코드 생성
│   │   └── prompt_optimizer.py # 프롬프트 최적화
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── generate.py         # 생성 API 라우터
│   │   └── health.py            # 헬스체크
│   └── utils/
│       ├── __init__.py
│       ├── cache.py             # 응답 캐시
│       └── queue.py             # 요청 큐
├── requirements.txt
├── .env.example
├── README.md
└── run.sh                       # 실행 스크립트
```

## 🔧 핵심 컴포넌트

### 1. Ollama Client (ollama_client.py)
- Ollama API 호출
- 모델 관리 및 전환
- 요청 큐 관리
- 응답 캐싱

### 2. Service Layer
- **TextGenerator**: 일반 텍스트 생성
- **CodeGenerator**: 코드 생성 (프로그래밍 언어별)
- **PromptOptimizer**: 프롬프트 최적화

### 3. API Router
- RESTful 엔드포인트
- 요청 검증
- 에러 처리
- 응답 포맷팅
