========================================
  🔧 에러 수정 후 업로드 명령어
========================================

프로젝트 폴더에서 다음 명령어를 순서대로 실행하세요:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Git 사용자 정보 설정 (필수!)

   git config --global user.email "partner0010@gmail.com"
   git config --global user.name "partner0010"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2. 파일 추가

   git add .

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3. 커밋

   git commit -m "Initial commit: Freeshell v2.0"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4. 브랜치 이름 변경

   git branch -M main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5. GitHub에 업로드

   git push -u origin main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 한 번에 복사해서 실행:

git config --global user.email "partner0010@gmail.com"
git config --global user.name "partner0010"
git add .
git commit -m "Initial commit: Freeshell v2.0"
git branch -M main
git push -u origin main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  인증이 필요할 수 있습니다:

git push 할 때 GitHub 로그인을 요구할 수 있습니다.
브라우저가 열리면 로그인하세요.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

