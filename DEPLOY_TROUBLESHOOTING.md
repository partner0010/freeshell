# 배포 문제 해결 가이드

## 🔧 일반적인 문제와 해결 방법

### 1. 'next' is not recognized as an internal or external command

**원인**: `node_modules`가 설치되지 않았거나 `next` 패키지가 없음

**해결 방법**:

1. **의존성 설치**:
   ```bash
   npm install
   ```

2. **캐시 정리 후 재설치**:
   ```bash
   npm cache clean --force
   rmdir /s /q node_modules
   rmdir /s /q package-lock.json
   npm install
   ```

3. **Node.js 버전 확인**:
   ```bash
   node --version
   npm --version
   ```
   - Node.js 18 이상 권장
   - npm 9 이상 권장

### 2. Git status 경고 메시지

**원인**: Git이 시스템 디렉토리를 스캔하려고 시도

**해결 방법**:
- 이 경고는 무시해도 됩니다
- `.gitignore` 파일이 올바르게 설정되어 있으면 문제 없음

### 3. Build failed 오류

**원인**: 여러 가지 가능성

**해결 방법**:

1. **TypeScript 오류 확인**:
   ```bash
   npx tsc --noEmit
   ```

2. **.next 폴더 삭제 후 재빌드**:
   ```bash
   rmdir /s /q .next
   npm run build
   ```

3. **의존성 재설치**:
   ```bash
   rmdir /s /q node_modules
   npm install
   npm run build
   ```

### 4. npm install 실패

**원인**: 네트워크 문제, 권한 문제, 또는 패키지 충돌

**해결 방법**:

1. **관리자 권한으로 실행**:
   - PowerShell 또는 CMD를 관리자 권한으로 실행
   - 배포 스크립트 실행

2. **npm 캐시 정리**:
   ```bash
   npm cache clean --force
   ```

3. **레거시 피어 의존성 사용**:
   ```bash
   npm install --legacy-peer-deps
   ```

### 5. 메모리 부족 오류

**원인**: 빌드 시 메모리 부족

**해결 방법**:

1. **Node.js 메모리 제한 증가**:
   ```bash
   set NODE_OPTIONS=--max-old-space-size=4096
   npm run build
   ```

2. **배치 파일에서 자동 설정됨**:
   - `deploy.bat`에서 이미 설정되어 있음

### 6. Git push 실패

**원인**: 원격 저장소 연결 문제, 인증 문제

**해결 방법**:

1. **원격 저장소 확인**:
   ```bash
   git remote -v
   ```

2. **브랜치 확인**:
   ```bash
   git branch
   ```

3. **인증 확인**:
   - GitHub Personal Access Token 필요
   - 또는 SSH 키 설정

## 📋 배포 전 체크리스트

### 필수 확인 사항

- [ ] Node.js 설치 확인 (`node --version`)
- [ ] npm 설치 확인 (`npm --version`)
- [ ] Git 설치 확인 (`git --version`)
- [ ] `.env.local` 파일 설정 (API 키 등)
- [ ] `node_modules` 폴더 존재 확인
- [ ] Git 저장소 연결 확인 (`git remote -v`)

### 권장 확인 사항

- [ ] TypeScript 오류 없음 (`npx tsc --noEmit`)
- [ ] 린터 오류 없음 (`npm run lint`)
- [ ] 로컬 빌드 성공 (`npm run build`)
- [ ] 로컬 서버 정상 작동 (`npm run dev`)

## 🚀 수동 배포 방법

배포 스크립트가 작동하지 않을 때:

### 1. 의존성 설치
```bash
cd "C:\Users\partn\OneDrive\바탕 화면\Cursor\Freeshell Update"
npm install
```

### 2. 빌드
```bash
npm run build
```

### 3. Git 커밋
```bash
git add .
git commit -m "Shell updates"
```

### 4. Git 푸시
```bash
git push origin main
```

## 🔍 디버깅 팁

### 로그 확인

1. **빌드 로그**:
   - 배포 스크립트 실행 시 터미널 출력 확인
   - 에러 메시지 자세히 읽기

2. **Git 로그**:
   ```bash
   git log --oneline -10
   ```

3. **npm 로그**:
   ```bash
   npm run build --verbose
   ```

### 환경 확인

1. **Node.js 버전**:
   ```bash
   node --version
   ```
   - 권장: v18.x 이상

2. **npm 버전**:
   ```bash
   npm --version
   ```
   - 권장: v9.x 이상

3. **디스크 공간**:
   - 최소 2GB 여유 공간 필요

## 📞 추가 도움

문제가 지속되면:

1. **에러 메시지 전체 복사**
2. **환경 정보 확인**:
   - Node.js 버전
   - npm 버전
   - 운영체제 버전
3. **GitHub Issues에 보고**

## ✅ 성공적인 배포 확인

배포가 성공하면:

1. **GitHub 저장소 확인**:
   - 최신 커밋 확인
   - 브랜치 상태 확인

2. **배포 플랫폼 확인**:
   - Netlify/Vercel 대시보드 확인
   - 배포 로그 확인
   - 도메인 접속 테스트

3. **기능 테스트**:
   - 메인 페이지 로드 확인
   - 로그인 기능 테스트
   - API 엔드포인트 테스트

